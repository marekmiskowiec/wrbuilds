---
const { frontmatter } = Astro.props;
import data from "../data/champion-stats.json"
---

<section class="champion-container">
    <div class="champion-stats">
        <div class="champion-image">
            <img src={frontmatter.image} alt="aatrox">
        </div>
        <div class="champion-info">
            <h2 class="champion-title">{frontmatter.title}</h2>
            <p class="champion-description">{frontmatter.description}</p>
            <p class="champion-tier">Tier: <span style="font-weight: bold;">{frontmatter.tier}</span></p>
        </div>
    </div>
    <section class="stats">
        <div class="container">
            <h2>Basic statistics</h2>
            <h1>{data[frontmatter.title].title} - {data[frontmatter.title].id}</h1>
            <p><strong>Title:</strong> {data[frontmatter.title].title}</p>
            <p><strong>Damage:</strong> {data[frontmatter.title].damage}</p>
            <p><strong>Toughness:</strong> {data[frontmatter.title].toughness}</p>
            <p><strong>Utility:</strong> {data[frontmatter.title].utility}</p>
            <p><strong>Difficulty:</strong> {data[frontmatter.title].difficulty}</p>
            <p><strong>Role:</strong> {data[frontmatter.title].role.join(', ')}</p>
            <p><strong>Skills:</strong> {data[frontmatter.title].skills.join(', ')}</p>
            <p><strong>Stats:</strong>{data[frontmatter.title].stats.hp_base}</p>
        </div>
    </section>



    <section class="items">
        <div class="container">
                <h2>{frontmatter.title} - item build</h2>
                <div class="row">
                    {frontmatter.builds.items.map((item) => (
                    <div class="item-container">
                        <div class="item-image">
                            <img src={`/items/${item}.webp`} alt="" />
                        </div>
                        <div class="item-description">
                            {item.replaceAll("-", " ")}
                        </div>
                    </div>
                    ))}
                </div>
            </div>
        </div>
    </section>

    <section class="summoner-spells">
        <div class="container">
                <h2>{frontmatter.title} - summoner spells</h2>
                <div class="row">
                    {frontmatter.builds.summoners.map((summoner) => (
                    <div class="item-container">
                        <div class="item-image">
                            <img src={`/summoners/${summoner}.png`} alt="" />
                        </div>
                        <div class="item-description">
                            {summoner.replaceAll("-", " ")}
                        </div>
                    </div>
                    ))}
                </div>
            </div>
        </div>
    </section>

    <section class="runes">
        <div class="container">
                <h2>{frontmatter.title} - runes</h2>
                <div class="row">
                    {frontmatter.builds.runes.map((rune) => (
                    <div class="item-container">
                        <div class="item-image">
                            <img src={`/runes/${rune}.png`} alt="" />
                        </div>
                        <div class="item-description">
                            {rune.replaceAll("-", " ")}
                        </div>
                    </div>
                    ))}
                </div>
            </div>
        </div>
    </section>

    <div class="skills">
        <h2>Abilities</h2>
        <h2>Passive</h2>
        <div class="skill">
            <div class="skill-video">
                <video src={frontmatter.abilities.passive.video} type="video/webm" autoplay muted loop playsinline />
            </div>
            <div class="skill-description">
                <p>{frontmatter.skilldescription1}</p>
            </div>
        </div>
        <h2>Skill one</h2>
        <div class="skill">
            <div class="skill-video">
                <video src={frontmatter.abilities.one.video} type="video/webm" autoplay muted loop playsinline />
            </div>
            <div class="skill-description">
                <p>{frontmatter.skilldescription1}</p>
            </div>
        </div>
        <h2>Skill two</h2>
        <div class="skill">
            <div class="skill-video">
                <video src={frontmatter.abilities.two.video} type="video/webm" autoplay muted loop playsinline />
            </div>
            <div class="skill-description">
                <p>{frontmatter.skilldescription1}</p>
            </div>
        </div>
        <h2>Skill three</h2>
        <div class="skill">
            <div class="skill-video">
                <video src={frontmatter.abilities.three.video} type="video/webm" autoplay muted loop playsinline />
            </div>
            <div class="skill-description">
                <p>{frontmatter.skilldescription1}</p>
            </div>
        </div>
        <h2>Skill four</h2>
        <div class="skill">
            <div class="skill-video">
                <video src={frontmatter.abilities.ult.video} type="video/webm" autoplay muted loop playsinline />
            </div>
            <div class="skill-description">
                <p>{frontmatter.skilldescription1}</p>
            </div>
        </div>
    </div>
</section>

<style>
    .container {
        background-color: #2e4da2;
        text-align: center;
        border-radius: 30px;
    }

    .row {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 50px;
    }

    .item-container {
        height: 150px;
        width: 100px;
        gap: 2;
    }

    .item-image img {
        width: 100%;
        border-radius: 50%;
    }

    .champion-container {
        width: 80%;
        margin: 0 auto;
        color: white;
    }

    .champion-stats {
        background-color: rgb(21, 200, 216);
        display: flex;
        gap: 100px;
        width: 100%;

    }

    .champion-image img {
        width: auto;
        height: 100%;
    }

    .champion-title {
        font-size: 40px;
        font-weight: bold;
    }

    .champion-description, .champion-tier {
        font-size: 20px;
    }

    .skill {
        display: flex;
    }

    .skill-video {
        flex: 1 0 50%;
    }

    .skill-video video {
        width: 100%;
    }

    .skill-description {
        flex: 1 0 50%;
        background-color: rgba(36, 227, 241, 0.687);
        font-size: 25px;

    }


    body {
        background-color: #172039;
    }

    /* Styl dla średnich ekranów (tablety) */
@media (max-width: 768px) {
    .skill {
        flex-direction: column; /* Ustawienie elementów w kolumnie */
        text-align: center; /* Wyśrodkowanie treści */
    }

    .skill-video,
    .skill-description {
        flex: 1 0 100%; /* Pełna szerokość dla obu sekcji */
    }

    .skill-description {
        font-size: 20px; /* Mniejsza czcionka dla tabletów */
    }
}

/* Styl dla małych ekranów (smartfony) */
@media (max-width: 480px) {
    .skill {
        flex-direction: column; /* Ustawienie elementów w kolumnie */
        margin: 10px 0;
    }

    .skill-video {
        flex: 1 0 100%; /* Pełna szerokość dla wideo */
    }

    .skill-video video {
        width: 100%; /* Dopasowanie wideo do kontenera */
    }

    .skill-description {
        flex: 1 0 100%; /* Pełna szerokość dla opisu */
        font-size: 18px; /* Jeszcze mniejsza czcionka dla telefonów */
        padding: 10px;
    }

    h2 {
        font-size: 22px; /* Zmniejszenie rozmiaru nagłówków */
    }
}
</style>